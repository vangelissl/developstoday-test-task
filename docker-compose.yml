services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./travel.db:/app/travel.db
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./travel.db